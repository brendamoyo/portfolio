- [ ] Unify theme for header/hero/about/services to match existing dark theme variables
- [ ] Make buttons/primary actions use theme variables (remove raw `blue`/hardcoded colors where inconsistent)
- [ ] Fix header navigation label (“Blog” -> correct section)
- [ ] Validate with `npm run build` and fix any compile/runtime errors
- [ ] Re-run build to confirm no errors

